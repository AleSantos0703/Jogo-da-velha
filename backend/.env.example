# PostgreSQL connection (example)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=jogo_velha

# JWT / security
JWT_SECRET=changeme
JWT_EXPIRES_IN=1h
BCRYPT_SALT_ROUNDS=10

# App
PORT=3000
